# wireguard-yaml

### Requirements:

- Python3

### How to setup

- Go to folder
- `virtualenv venv`
- `pip3 install --editable .`
- `. venv/bin/activate`
- `wireguard`

Also check `wireguard --help` for a list of options
